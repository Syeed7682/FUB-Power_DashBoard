services:
  - type: web
    name: fub-power-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      PORT: "10000"
